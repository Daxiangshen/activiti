spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/activiti?useSSL=false&nullCatalogMeansCurrent=true&serverTimezone=UTC
    username: root
    password: 123456
  activiti:
    # 不自动检查resources/bpmn目录
    check-process-definitions: false
server:
  port: 8002
